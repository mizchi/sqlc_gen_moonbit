// Generated by sqlc-gen-moonbit
// sqlc version: v1.30.0

///|
pub struct GetUserRow {
  id : Int64
  name : String
  email : String
  created_at : String?
} derive(Show, Eq)

///|
pub struct ListUsersRow {
  id : Int64
  name : String
  email : String
  created_at : String?
} derive(Show, Eq)

///|
pub struct GetUserParams {
  id : Int64
} derive(Show, Eq)

///|
pub fn GetUserParams::new(id : Int64) -> GetUserParams {
  { id, }
}

///|
pub struct CreateUserParams {
  name : String
  email : String
} derive(Show, Eq)

///|
pub fn CreateUserParams::new(name : String, email : String) -> CreateUserParams {
  { name, email, }
}

///|
pub struct UpdateUserParams {
  id : Int64
  name : String
  email : String
} derive(Show, Eq)

///|
pub fn UpdateUserParams::new(id : Int64, name : String, email : String) -> UpdateUserParams {
  { id, name, email, }
}

///|
pub struct DeleteUserParams {
  id : Int64
} derive(Show, Eq)

///|
pub fn DeleteUserParams::new(id : Int64) -> DeleteUserParams {
  { id, }
}

// SQL queries
pub let get_user_sql : String = "SELECT id, name, email, created_at FROM users WHERE id = $1"

pub let list_users_sql : String = "SELECT id, name, email, created_at FROM users ORDER BY id"

pub let create_user_sql : String = "INSERT INTO users (name, email) VALUES ($1, $2) RETURNING id"

pub let update_user_sql : String = "UPDATE users SET name = $2, email = $3 WHERE id = $1"

pub let delete_user_sql : String = "DELETE FROM users WHERE id = $1"

