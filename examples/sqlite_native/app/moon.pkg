import {
  "mizchi/sqlite",
  "moonbitlang/x/encoding",
  "example_sqlite_native/db/gen",
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  link: { "native": { "cc-link-flags": "-lsqlite3" } },
)
