// Generated by sqlc-gen-moonbit (backend: d1)
// Requires: mizchi/cloudflare

///| Read a required (NOT NULL) column from a D1 result row.
/// Aborts the request with a clear message if the cell is
/// null or missing — surfacing the bad row at the read site
/// instead of letting `undefined` propagate through `cast()`
/// into a MoonBit value.
fn d1_require_field(row : @core.Any, field : String) -> @core.Any {
  let value = row[field]
  if @core.is_null(value) || @core.typeof_(value) == "undefined" {
    abort("D1 row missing required column \"" + field + "\"")
  }
  value
}

///| Convert MoonBit Int64 params to a JavaScript Number before
/// binding. Cloudflare D1 can hang instead of rejecting when a
/// JS BigInt reaches `D1PreparedStatement.bind(...)`.
extern "js" fn d1_bind_int64(value : Int64) -> @core.Any =
  #| (value) => Number(value)

///| Convert a JavaScript D1 integer cell to MoonBit Int64.
extern "js" fn d1_read_int64(value : @core.Any) -> Int64 =
  #| (value) => BigInt(Math.trunc(Number(value)))

///| Convert a JavaScript D1 integer cell to MoonBit Int.
extern "js" fn d1_read_int(value : @core.Any) -> Int =
  #| (value) => Number(value) | 0

///| Expand a sqlc.slice('name') placeholder to a comma-separated
/// list of `?` placeholders matching the slice length. An empty
/// slice expands to `NULL` so `WHERE col IN (NULL)` short-circuits
/// to no rows — saner than emitting an invalid `IN ()` clause.
fn sqlc_slice_placeholders(n : Int) -> String {
  if n <= 0 {
    return "NULL"
  }
  let buf = StringBuilder()
  for i = 0; i < n; i = i + 1 {
    if i > 0 {
      buf.write_string(",")
    }
    buf.write_string("?")
  }
  buf.to_string()
}

// Query functions
///| GetUserById
///
/// SELECT id, username, email, display_name, bio, avatar_url, created_at, updated_at FROM users WHERE id = ?
pub async fn get_user_by_id(db : @cloudflare.D1Database, params : GetUserByIdParams) -> GetUserByIdRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_user_by_id_sql).bind([d1_bind_int64(params.id)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      username: d1_require_field(result, "username").cast(),
      email: d1_require_field(result, "email").cast(),
      display_name: d1_require_field(result, "display_name").cast(),
      bio: (if @core.is_null(result["bio"]) || @core.typeof_(result["bio"]) == "undefined" { None } else { Some(result["bio"].cast()) }),
      avatar_url: (if @core.is_null(result["avatar_url"]) || @core.typeof_(result["avatar_url"]) == "undefined" { None } else { Some(result["avatar_url"].cast()) }),
      created_at: d1_require_field(result, "created_at").cast(),
      updated_at: d1_require_field(result, "updated_at").cast(),
    })
  }
}

///| GetUserByUsername
/// SELECT id, username, email, display_name, bio, avatar_url, created_at, updated_at FROM users WHERE username = ?
pub async fn get_user_by_username(db : @cloudflare.D1Database, params : GetUserByUsernameParams) -> GetUserByUsernameRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_user_by_username_sql).bind([@core.any(params.username)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      username: d1_require_field(result, "username").cast(),
      email: d1_require_field(result, "email").cast(),
      display_name: d1_require_field(result, "display_name").cast(),
      bio: (if @core.is_null(result["bio"]) || @core.typeof_(result["bio"]) == "undefined" { None } else { Some(result["bio"].cast()) }),
      avatar_url: (if @core.is_null(result["avatar_url"]) || @core.typeof_(result["avatar_url"]) == "undefined" { None } else { Some(result["avatar_url"].cast()) }),
      created_at: d1_require_field(result, "created_at").cast(),
      updated_at: d1_require_field(result, "updated_at").cast(),
    })
  }
}

///| ListUsers
/// SELECT id, username, email, display_name, bio, avatar_url, created_at, updated_at FROM users ORDER BY created_at DESC
pub async fn list_users(db : @cloudflare.D1Database) -> Array[ListUsersRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_users_sql)
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListUsersRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      username: d1_require_field(row, "username").cast(),
      email: d1_require_field(row, "email").cast(),
      display_name: d1_require_field(row, "display_name").cast(),
      bio: (if @core.is_null(row["bio"]) || @core.typeof_(row["bio"]) == "undefined" { None } else { Some(row["bio"].cast()) }),
      avatar_url: (if @core.is_null(row["avatar_url"]) || @core.typeof_(row["avatar_url"]) == "undefined" { None } else { Some(row["avatar_url"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      updated_at: d1_require_field(row, "updated_at").cast(),
    })
  }
  results
}

///| CreateUser
/// INSERT INTO users (username, email, display_name, bio, avatar_url)
/// VALUES (?, ?, ?, ?, ?)
pub async fn create_user(db : @cloudflare.D1Database, params : CreateUserParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(create_user_sql).bind([@core.any(params.username), @core.any(params.email), @core.any(params.display_name), (match params.bio { Some(v) => @core.any(v); None => @core.null() }), (match params.avatar_url { Some(v) => @core.any(v); None => @core.null() })])
  stmt.run() |> ignore
}

///| CreateUserReturningId
/// INSERT INTO users (username, email, display_name, bio, avatar_url)
/// VALUES (?, ?, ?, ?, ?)
pub async fn create_user_returning_id(db : @cloudflare.D1Database, params : CreateUserReturningIdParams) -> Int64 raise @cloudflare.D1Error {
  let stmt = db.prepare(create_user_returning_id_sql).bind([@core.any(params.username), @core.any(params.email), @core.any(params.display_name), (match params.bio { Some(v) => @core.any(v); None => @core.null() }), (match params.avatar_url { Some(v) => @core.any(v); None => @core.null() })])
  let result = stmt.run()
  match result.meta() {
    Some(m) => m.last_row_id().unwrap_or(0).to_int64()
    None => 0L
  }
}

///| DeleteUserById
/// DELETE FROM users WHERE id = ?
pub async fn delete_user_by_id(db : @cloudflare.D1Database, params : DeleteUserByIdParams) -> Int raise @cloudflare.D1Error {
  let stmt = db.prepare(delete_user_by_id_sql).bind([d1_bind_int64(params.id)])
  let result = stmt.run()
  match result.meta() {
    Some(m) => m.changes().unwrap_or(0)
    None => 0
  }
}

///| UpdateUserDisplayName
/// UPDATE users SET display_name = ? WHERE id = ?
pub async fn update_user_display_name(db : @cloudflare.D1Database, params : UpdateUserDisplayNameParams) -> Int raise @cloudflare.D1Error {
  let stmt = db.prepare(update_user_display_name_sql).bind([@core.any(params.display_name), d1_bind_int64(params.id)])
  let result = stmt.run()
  match result.meta() {
    Some(m) => m.changes().unwrap_or(0)
    None => 0
  }
}

///| GetCategoryById
///
/// SELECT id, name, slug, description FROM categories WHERE id = ?
pub async fn get_category_by_id(db : @cloudflare.D1Database, params : GetCategoryByIdParams) -> GetCategoryByIdRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_category_by_id_sql).bind([d1_bind_int64(params.id)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      name: d1_require_field(result, "name").cast(),
      slug: d1_require_field(result, "slug").cast(),
      description: (if @core.is_null(result["description"]) || @core.typeof_(result["description"]) == "undefined" { None } else { Some(result["description"].cast()) }),
    })
  }
}

///| ListCategories
/// SELECT id, name, slug, description FROM categories ORDER BY name
pub async fn list_categories(db : @cloudflare.D1Database) -> Array[ListCategoriesRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_categories_sql)
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListCategoriesRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      name: d1_require_field(row, "name").cast(),
      slug: d1_require_field(row, "slug").cast(),
      description: (if @core.is_null(row["description"]) || @core.typeof_(row["description"]) == "undefined" { None } else { Some(row["description"].cast()) }),
    })
  }
  results
}

///| CreateCategory
/// INSERT INTO categories (name, slug, description) VALUES (?, ?, ?)
pub async fn create_category(db : @cloudflare.D1Database, params : CreateCategoryParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(create_category_sql).bind([@core.any(params.name), @core.any(params.slug), (match params.description { Some(v) => @core.any(v); None => @core.null() })])
  stmt.run() |> ignore
}

///| GetPostById
///
/// SELECT id, author_id, category_id, title, slug, content, excerpt, status, published_at, created_at, updated_at FROM posts WHERE id = ?
pub async fn get_post_by_id(db : @cloudflare.D1Database, params : GetPostByIdParams) -> GetPostByIdRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_post_by_id_sql).bind([d1_bind_int64(params.id)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      author_id: d1_read_int64(d1_require_field(result, "author_id")),
      category_id: (if @core.is_null(result["category_id"]) || @core.typeof_(result["category_id"]) == "undefined" { None } else { Some(d1_read_int64(result["category_id"])) }),
      title: d1_require_field(result, "title").cast(),
      slug: d1_require_field(result, "slug").cast(),
      content: d1_require_field(result, "content").cast(),
      excerpt: (if @core.is_null(result["excerpt"]) || @core.typeof_(result["excerpt"]) == "undefined" { None } else { Some(result["excerpt"].cast()) }),
      status: d1_require_field(result, "status").cast(),
      published_at: (if @core.is_null(result["published_at"]) || @core.typeof_(result["published_at"]) == "undefined" { None } else { Some(result["published_at"].cast()) }),
      created_at: d1_require_field(result, "created_at").cast(),
      updated_at: d1_require_field(result, "updated_at").cast(),
    })
  }
}

///| GetPostBySlug
/// SELECT id, author_id, category_id, title, slug, content, excerpt, status, published_at, created_at, updated_at FROM posts WHERE slug = ?
pub async fn get_post_by_slug(db : @cloudflare.D1Database, params : GetPostBySlugParams) -> GetPostBySlugRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_post_by_slug_sql).bind([@core.any(params.slug)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      author_id: d1_read_int64(d1_require_field(result, "author_id")),
      category_id: (if @core.is_null(result["category_id"]) || @core.typeof_(result["category_id"]) == "undefined" { None } else { Some(d1_read_int64(result["category_id"])) }),
      title: d1_require_field(result, "title").cast(),
      slug: d1_require_field(result, "slug").cast(),
      content: d1_require_field(result, "content").cast(),
      excerpt: (if @core.is_null(result["excerpt"]) || @core.typeof_(result["excerpt"]) == "undefined" { None } else { Some(result["excerpt"].cast()) }),
      status: d1_require_field(result, "status").cast(),
      published_at: (if @core.is_null(result["published_at"]) || @core.typeof_(result["published_at"]) == "undefined" { None } else { Some(result["published_at"].cast()) }),
      created_at: d1_require_field(result, "created_at").cast(),
      updated_at: d1_require_field(result, "updated_at").cast(),
    })
  }
}

///| ListPublishedPosts
/// SELECT id, author_id, category_id, title, slug, content, excerpt, status, published_at, created_at, updated_at FROM posts
/// WHERE status = 'published'
/// ORDER BY published_at DESC
/// LIMIT ?
pub async fn list_published_posts(db : @cloudflare.D1Database, params : ListPublishedPostsParams) -> Array[ListPublishedPostsRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_published_posts_sql).bind([d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPublishedPostsRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      category_id: (if @core.is_null(row["category_id"]) || @core.typeof_(row["category_id"]) == "undefined" { None } else { Some(d1_read_int64(row["category_id"])) }),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      content: d1_require_field(row, "content").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      updated_at: d1_require_field(row, "updated_at").cast(),
    })
  }
  results
}

///| ListPostsByAuthor
/// SELECT id, author_id, category_id, title, slug, content, excerpt, status, published_at, created_at, updated_at FROM posts
/// WHERE author_id = ?
/// ORDER BY created_at DESC
pub async fn list_posts_by_author(db : @cloudflare.D1Database, params : ListPostsByAuthorParams) -> Array[ListPostsByAuthorRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_posts_by_author_sql).bind([d1_bind_int64(params.author_id)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPostsByAuthorRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      category_id: (if @core.is_null(row["category_id"]) || @core.typeof_(row["category_id"]) == "undefined" { None } else { Some(d1_read_int64(row["category_id"])) }),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      content: d1_require_field(row, "content").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      updated_at: d1_require_field(row, "updated_at").cast(),
    })
  }
  results
}

///| CreatePost
/// INSERT INTO posts (author_id, category_id, title, slug, content, excerpt, status)
/// VALUES (?, ?, ?, ?, ?, ?, ?)
pub async fn create_post(db : @cloudflare.D1Database, params : CreatePostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(create_post_sql).bind([d1_bind_int64(params.author_id), (match params.category_id { Some(v) => d1_bind_int64(v); None => @core.null() }), @core.any(params.title), @core.any(params.slug), @core.any(params.content), (match params.excerpt { Some(v) => @core.any(v); None => @core.null() }), @core.any(params.status)])
  stmt.run() |> ignore
}

///| PublishPost
/// UPDATE posts
/// SET status = 'published', published_at = datetime('now'), updated_at = datetime('now')
/// WHERE id = ?
pub async fn publish_post(db : @cloudflare.D1Database, params : PublishPostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(publish_post_sql).bind([d1_bind_int64(params.id)])
  stmt.run() |> ignore
}

///| DeletePost
/// DELETE FROM posts WHERE id = ?
pub async fn delete_post(db : @cloudflare.D1Database, params : DeletePostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(delete_post_sql).bind([d1_bind_int64(params.id)])
  stmt.run() |> ignore
}

///| ListTags
///
/// SELECT id, name, slug FROM tags ORDER BY name
pub async fn list_tags(db : @cloudflare.D1Database) -> Array[ListTagsRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_tags_sql)
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListTagsRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      name: d1_require_field(row, "name").cast(),
      slug: d1_require_field(row, "slug").cast(),
    })
  }
  results
}

///| CreateTag
/// INSERT INTO tags (name, slug) VALUES (?, ?)
pub async fn create_tag(db : @cloudflare.D1Database, params : CreateTagParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(create_tag_sql).bind([@core.any(params.name), @core.any(params.slug)])
  stmt.run() |> ignore
}

///| UpsertTagWithSqlcArg
/// INSERT INTO tags (name, slug) VALUES (?, ?)
/// ON CONFLICT (slug) DO UPDATE SET
///   name = CASE WHEN ?3 <> '' THEN ?3 ELSE name END
pub async fn upsert_tag_with_sqlc_arg(db : @cloudflare.D1Database, params : UpsertTagWithSqlcArgParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(upsert_tag_with_sqlc_arg_sql).bind([@core.any(params.name), @core.any(params.slug), @core.any(params.new_name)])
  stmt.run() |> ignore
}

///| CreateTagWithBodyComments
/// INSERT INTO tags (name, slug) VALUES (?, ?)
pub async fn create_tag_with_body_comments(db : @cloudflare.D1Database, params : CreateTagWithBodyCommentsParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(create_tag_with_body_comments_sql).bind([@core.any(params.name), @core.any(params.slug)])
  stmt.run() |> ignore
}

///| DeleteTagAfterBodyComments
/// DELETE FROM tags WHERE slug = ?
pub async fn delete_tag_after_body_comments(db : @cloudflare.D1Database, params : DeleteTagAfterBodyCommentsParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(delete_tag_after_body_comments_sql).bind([@core.any(params.slug)])
  stmt.run() |> ignore
}

///| ListCommentsForPost
///
/// SELECT id, post_id, author_name, author_email, content, is_approved, created_at FROM comments
/// WHERE post_id = ? AND is_approved = 1
/// ORDER BY created_at ASC
pub async fn list_comments_for_post(db : @cloudflare.D1Database, params : ListCommentsForPostParams) -> Array[ListCommentsForPostRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_comments_for_post_sql).bind([d1_bind_int64(params.post_id)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListCommentsForPostRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      post_id: d1_read_int64(d1_require_field(row, "post_id")),
      author_name: d1_require_field(row, "author_name").cast(),
      author_email: d1_require_field(row, "author_email").cast(),
      content: d1_require_field(row, "content").cast(),
      is_approved: d1_read_int64(d1_require_field(row, "is_approved")),
      created_at: d1_require_field(row, "created_at").cast(),
    })
  }
  results
}

///| CreateComment
/// INSERT INTO comments (post_id, author_name, author_email, content)
/// VALUES (?, ?, ?, ?)
pub async fn create_comment(db : @cloudflare.D1Database, params : CreateCommentParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(create_comment_sql).bind([d1_bind_int64(params.post_id), @core.any(params.author_name), @core.any(params.author_email), @core.any(params.content)])
  stmt.run() |> ignore
}

///| ListCommentAggregateCandidates
/// SELECT
///   post_id,
///   COUNT(id) AS comment_count,
///   COALESCE(SUM(is_approved), 0) AS approved_count
/// FROM comments
/// GROUP BY post_id
/// HAVING COUNT(id) >= ?1
///    OR COALESCE(SUM(is_approved), 0) >= ?2
pub async fn list_comment_aggregate_candidates(db : @cloudflare.D1Database, params : ListCommentAggregateCandidatesParams) -> Array[ListCommentAggregateCandidatesRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_comment_aggregate_candidates_sql).bind([d1_bind_int64(params.min_comments), d1_bind_int64(params.min_approved)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListCommentAggregateCandidatesRow] = []
  for row in results_arr {
    results.push({
      post_id: d1_read_int64(d1_require_field(row, "post_id")),
      comment_count: d1_read_int64(d1_require_field(row, "comment_count")),
      approved_count: d1_read_int64(d1_require_field(row, "approved_count")),
    })
  }
  results
}

///| GetPostWithAuthor
///
/// SELECT
///   p.id, p.title, p.slug, p.content, p.excerpt, p.status,
///   p.published_at, p.created_at, p.updated_at,
///   u.id as author_id, u.username as author_username,
///   u.display_name as author_display_name, u.avatar_url as author_avatar_url
/// FROM posts p
/// INNER JOIN users u ON p.author_id = u.id
/// WHERE p.id = ?
pub async fn get_post_with_author(db : @cloudflare.D1Database, params : GetPostWithAuthorParams) -> GetPostWithAuthorRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_post_with_author_sql).bind([d1_bind_int64(params.id)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      title: d1_require_field(result, "title").cast(),
      slug: d1_require_field(result, "slug").cast(),
      content: d1_require_field(result, "content").cast(),
      excerpt: (if @core.is_null(result["excerpt"]) || @core.typeof_(result["excerpt"]) == "undefined" { None } else { Some(result["excerpt"].cast()) }),
      status: d1_require_field(result, "status").cast(),
      published_at: (if @core.is_null(result["published_at"]) || @core.typeof_(result["published_at"]) == "undefined" { None } else { Some(result["published_at"].cast()) }),
      created_at: d1_require_field(result, "created_at").cast(),
      updated_at: d1_require_field(result, "updated_at").cast(),
      author_id: d1_read_int64(d1_require_field(result, "author_id")),
      author_username: d1_require_field(result, "author_username").cast(),
      author_display_name: d1_require_field(result, "author_display_name").cast(),
      author_avatar_url: (if @core.is_null(result["author_avatar_url"]) || @core.typeof_(result["author_avatar_url"]) == "undefined" { None } else { Some(result["author_avatar_url"].cast()) }),
    })
  }
}

///| GetPostWithAuthorBySlug
/// SELECT
///   p.id, p.title, p.slug, p.content, p.excerpt, p.status,
///   p.published_at, p.created_at, p.updated_at,
///   u.id as author_id, u.username as author_username,
///   u.display_name as author_display_name, u.avatar_url as author_avatar_url
/// FROM posts p
/// INNER JOIN users u ON p.author_id = u.id
/// WHERE p.slug = ?
pub async fn get_post_with_author_by_slug(db : @cloudflare.D1Database, params : GetPostWithAuthorBySlugParams) -> GetPostWithAuthorBySlugRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_post_with_author_by_slug_sql).bind([@core.any(params.slug)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      title: d1_require_field(result, "title").cast(),
      slug: d1_require_field(result, "slug").cast(),
      content: d1_require_field(result, "content").cast(),
      excerpt: (if @core.is_null(result["excerpt"]) || @core.typeof_(result["excerpt"]) == "undefined" { None } else { Some(result["excerpt"].cast()) }),
      status: d1_require_field(result, "status").cast(),
      published_at: (if @core.is_null(result["published_at"]) || @core.typeof_(result["published_at"]) == "undefined" { None } else { Some(result["published_at"].cast()) }),
      created_at: d1_require_field(result, "created_at").cast(),
      updated_at: d1_require_field(result, "updated_at").cast(),
      author_id: d1_read_int64(d1_require_field(result, "author_id")),
      author_username: d1_require_field(result, "author_username").cast(),
      author_display_name: d1_require_field(result, "author_display_name").cast(),
      author_avatar_url: (if @core.is_null(result["author_avatar_url"]) || @core.typeof_(result["author_avatar_url"]) == "undefined" { None } else { Some(result["author_avatar_url"].cast()) }),
    })
  }
}

///| ListPostsWithAuthors
/// SELECT
///   p.id, p.title, p.slug, p.excerpt, p.status,
///   p.published_at, p.created_at,
///   u.id as author_id, u.username as author_username,
///   u.display_name as author_display_name
/// FROM posts p
/// INNER JOIN users u ON p.author_id = u.id
/// WHERE p.status = 'published'
/// ORDER BY p.published_at DESC
/// LIMIT ?
pub async fn list_posts_with_authors(db : @cloudflare.D1Database, params : ListPostsWithAuthorsParams) -> Array[ListPostsWithAuthorsRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_posts_with_authors_sql).bind([d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPostsWithAuthorsRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      author_username: d1_require_field(row, "author_username").cast(),
      author_display_name: d1_require_field(row, "author_display_name").cast(),
    })
  }
  results
}

///| GetPostWithCategory
/// SELECT
///   p.id, p.title, p.slug, p.content, p.status,
///   c.id as category_id, c.name as category_name, c.slug as category_slug
/// FROM posts p
/// LEFT JOIN categories c ON p.category_id = c.id
/// WHERE p.id = ?
pub async fn get_post_with_category(db : @cloudflare.D1Database, params : GetPostWithCategoryParams) -> GetPostWithCategoryRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_post_with_category_sql).bind([d1_bind_int64(params.id)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      title: d1_require_field(result, "title").cast(),
      slug: d1_require_field(result, "slug").cast(),
      content: d1_require_field(result, "content").cast(),
      status: d1_require_field(result, "status").cast(),
      category_id: (if @core.is_null(result["category_id"]) || @core.typeof_(result["category_id"]) == "undefined" { None } else { Some(d1_read_int64(result["category_id"])) }),
      category_name: (if @core.is_null(result["category_name"]) || @core.typeof_(result["category_name"]) == "undefined" { None } else { Some(result["category_name"].cast()) }),
      category_slug: (if @core.is_null(result["category_slug"]) || @core.typeof_(result["category_slug"]) == "undefined" { None } else { Some(result["category_slug"].cast()) }),
    })
  }
}

///| ListPostsWithDetails
/// SELECT
///   p.id, p.title, p.slug, p.excerpt, p.status, p.published_at,
///   u.id as author_id, u.username as author_username, u.display_name as author_display_name,
///   c.id as category_id, c.name as category_name
/// FROM posts p
/// INNER JOIN users u ON p.author_id = u.id
/// LEFT JOIN categories c ON p.category_id = c.id
/// WHERE p.status = 'published'
/// ORDER BY p.published_at DESC
/// LIMIT ?
pub async fn list_posts_with_details(db : @cloudflare.D1Database, params : ListPostsWithDetailsParams) -> Array[ListPostsWithDetailsRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_posts_with_details_sql).bind([d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPostsWithDetailsRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      author_username: d1_require_field(row, "author_username").cast(),
      author_display_name: d1_require_field(row, "author_display_name").cast(),
      category_id: (if @core.is_null(row["category_id"]) || @core.typeof_(row["category_id"]) == "undefined" { None } else { Some(d1_read_int64(row["category_id"])) }),
      category_name: (if @core.is_null(row["category_name"]) || @core.typeof_(row["category_name"]) == "undefined" { None } else { Some(row["category_name"].cast()) }),
    })
  }
  results
}

///| ListTagsForPost
/// SELECT t.id, t.name, t.slug
/// FROM tags t
/// INNER JOIN post_tags pt ON t.id = pt.tag_id
/// WHERE pt.post_id = ?
/// ORDER BY t.name
pub async fn list_tags_for_post(db : @cloudflare.D1Database, params : ListTagsForPostParams) -> Array[ListTagsForPostRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_tags_for_post_sql).bind([d1_bind_int64(params.post_id)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListTagsForPostRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      name: d1_require_field(row, "name").cast(),
      slug: d1_require_field(row, "slug").cast(),
    })
  }
  results
}

///| ListPostsWithTagCount
/// SELECT
///   p.id, p.title, p.slug, p.status,
///   COUNT(pt.tag_id) as tag_count
/// FROM posts p
/// LEFT JOIN post_tags pt ON p.id = pt.post_id
/// GROUP BY p.id
/// ORDER BY tag_count DESC
/// LIMIT ?
pub async fn list_posts_with_tag_count(db : @cloudflare.D1Database, params : ListPostsWithTagCountParams) -> Array[ListPostsWithTagCountRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_posts_with_tag_count_sql).bind([d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPostsWithTagCountRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      status: d1_require_field(row, "status").cast(),
      tag_count: d1_read_int64(d1_require_field(row, "tag_count")),
    })
  }
  results
}

///| GetPostCommentsWithCount
/// SELECT
///   p.id, p.title,
///   COUNT(c.id) as comment_count,
///   SUM(CASE WHEN c.is_approved = 1 THEN 1 ELSE 0 END) as approved_count
/// FROM posts p
/// LEFT JOIN comments c ON p.id = c.post_id
/// WHERE p.id = ?
/// GROUP BY p.id
pub async fn get_post_comments_with_count(db : @cloudflare.D1Database, params : GetPostCommentsWithCountParams) -> GetPostCommentsWithCountRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_post_comments_with_count_sql).bind([d1_bind_int64(params.id)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      title: d1_require_field(result, "title").cast(),
      comment_count: d1_read_int64(d1_require_field(result, "comment_count")),
      approved_count: (if @core.is_null(result["approved_count"]) || @core.typeof_(result["approved_count"]) == "undefined" { None } else { Some(result["approved_count"].cast()) }),
    })
  }
}

///| AddTagToPost
///
/// INSERT INTO post_tags (post_id, tag_id) VALUES (?, ?)
pub async fn add_tag_to_post(db : @cloudflare.D1Database, params : AddTagToPostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(add_tag_to_post_sql).bind([d1_bind_int64(params.post_id), d1_bind_int64(params.tag_id)])
  stmt.run() |> ignore
}

///| RemoveTagFromPost
/// DELETE FROM post_tags WHERE post_id = ? AND tag_id = ?
pub async fn remove_tag_from_post(db : @cloudflare.D1Database, params : RemoveTagFromPostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(remove_tag_from_post_sql).bind([d1_bind_int64(params.post_id), d1_bind_int64(params.tag_id)])
  stmt.run() |> ignore
}

///| RemoveAllTagsFromPost
/// DELETE FROM post_tags WHERE post_id = ?
pub async fn remove_all_tags_from_post(db : @cloudflare.D1Database, params : RemoveAllTagsFromPostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(remove_all_tags_from_post_sql).bind([d1_bind_int64(params.post_id)])
  stmt.run() |> ignore
}

///| GetOrCreateTag
/// INSERT INTO tags (name, slug) VALUES (?, ?)
/// ON CONFLICT (slug) DO UPDATE SET name = excluded.name
/// RETURNING id, name, slug
pub async fn get_or_create_tag(db : @cloudflare.D1Database, params : GetOrCreateTagParams) -> GetOrCreateTagRow? raise @cloudflare.D1Error {
  let stmt = db.prepare(get_or_create_tag_sql).bind([@core.any(params.name), @core.any(params.slug)])
  match stmt.first() {
    None => None
    Some(result) => Some({
      id: d1_read_int64(d1_require_field(result, "id")),
      name: d1_require_field(result, "name").cast(),
      slug: d1_require_field(result, "slug").cast(),
    })
  }
}

///| ApproveComment
/// UPDATE comments SET is_approved = 1 WHERE id = ?
pub async fn approve_comment(db : @cloudflare.D1Database, params : ApproveCommentParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(approve_comment_sql).bind([d1_bind_int64(params.id)])
  stmt.run() |> ignore
}

///| ApproveAllCommentsForPost
/// UPDATE comments SET is_approved = 1 WHERE post_id = ?
pub async fn approve_all_comments_for_post(db : @cloudflare.D1Database, params : ApproveAllCommentsForPostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(approve_all_comments_for_post_sql).bind([d1_bind_int64(params.post_id)])
  stmt.run() |> ignore
}

///| DeleteAllCommentsForPost
/// DELETE FROM comments WHERE post_id = ?
pub async fn delete_all_comments_for_post(db : @cloudflare.D1Database, params : DeleteAllCommentsForPostParams) -> Unit raise @cloudflare.D1Error {
  let stmt = db.prepare(delete_all_comments_for_post_sql).bind([d1_bind_int64(params.post_id)])
  stmt.run() |> ignore
}

///| ListUsersPaginated
///
/// SELECT id, username, email, display_name, bio, avatar_url, created_at, updated_at FROM users WHERE id > ? ORDER BY id LIMIT ?
pub async fn list_users_paginated(db : @cloudflare.D1Database, params : ListUsersPaginatedParams) -> Array[ListUsersPaginatedRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_users_paginated_sql).bind([d1_bind_int64(params.id), d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListUsersPaginatedRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      username: d1_require_field(row, "username").cast(),
      email: d1_require_field(row, "email").cast(),
      display_name: d1_require_field(row, "display_name").cast(),
      bio: (if @core.is_null(row["bio"]) || @core.typeof_(row["bio"]) == "undefined" { None } else { Some(row["bio"].cast()) }),
      avatar_url: (if @core.is_null(row["avatar_url"]) || @core.typeof_(row["avatar_url"]) == "undefined" { None } else { Some(row["avatar_url"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      updated_at: d1_require_field(row, "updated_at").cast(),
    })
  }
  results
}

///| ListPostsPaginated
/// SELECT id, author_id, category_id, title, slug, content, excerpt, status, published_at, created_at, updated_at FROM posts WHERE id > ? ORDER BY id LIMIT ?
pub async fn list_posts_paginated(db : @cloudflare.D1Database, params : ListPostsPaginatedParams) -> Array[ListPostsPaginatedRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_posts_paginated_sql).bind([d1_bind_int64(params.id), d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPostsPaginatedRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      category_id: (if @core.is_null(row["category_id"]) || @core.typeof_(row["category_id"]) == "undefined" { None } else { Some(d1_read_int64(row["category_id"])) }),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      content: d1_require_field(row, "content").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      updated_at: d1_require_field(row, "updated_at").cast(),
    })
  }
  results
}

///| ListPublishedPostsPaginated
/// SELECT id, author_id, category_id, title, slug, content, excerpt, status, published_at, created_at, updated_at FROM posts
/// WHERE status = 'published' AND id > ?
/// ORDER BY id
/// LIMIT ?
pub async fn list_published_posts_paginated(db : @cloudflare.D1Database, params : ListPublishedPostsPaginatedParams) -> Array[ListPublishedPostsPaginatedRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_published_posts_paginated_sql).bind([d1_bind_int64(params.id), d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPublishedPostsPaginatedRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      category_id: (if @core.is_null(row["category_id"]) || @core.typeof_(row["category_id"]) == "undefined" { None } else { Some(d1_read_int64(row["category_id"])) }),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      content: d1_require_field(row, "content").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      updated_at: d1_require_field(row, "updated_at").cast(),
    })
  }
  results
}

///| ListPostsByAuthorPaginated
/// SELECT id, author_id, category_id, title, slug, content, excerpt, status, published_at, created_at, updated_at FROM posts
/// WHERE author_id = ? AND id > ?
/// ORDER BY id
/// LIMIT ?
pub async fn list_posts_by_author_paginated(db : @cloudflare.D1Database, params : ListPostsByAuthorPaginatedParams) -> Array[ListPostsByAuthorPaginatedRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_posts_by_author_paginated_sql).bind([d1_bind_int64(params.author_id), d1_bind_int64(params.id), d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPostsByAuthorPaginatedRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      category_id: (if @core.is_null(row["category_id"]) || @core.typeof_(row["category_id"]) == "undefined" { None } else { Some(d1_read_int64(row["category_id"])) }),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      content: d1_require_field(row, "content").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      updated_at: d1_require_field(row, "updated_at").cast(),
    })
  }
  results
}

///| ListPostsWithAuthorsPaginated
/// SELECT
///   p.id, p.title, p.slug, p.excerpt, p.status,
///   p.published_at, p.created_at,
///   u.id as author_id, u.username as author_username,
///   u.display_name as author_display_name
/// FROM posts p
/// INNER JOIN users u ON p.author_id = u.id
/// WHERE p.status = 'published' AND p.id > ?
/// ORDER BY p.id
/// LIMIT ?
pub async fn list_posts_with_authors_paginated(db : @cloudflare.D1Database, params : ListPostsWithAuthorsPaginatedParams) -> Array[ListPostsWithAuthorsPaginatedRow] raise @cloudflare.D1Error {
  let stmt = db.prepare(list_posts_with_authors_paginated_sql).bind([d1_bind_int64(params.id), d1_bind_int64(params.limit)])
  let d1_result = stmt.all()
  let results_arr = d1_result.get_results()
  let results : Array[ListPostsWithAuthorsPaginatedRow] = []
  for row in results_arr {
    results.push({
      id: d1_read_int64(d1_require_field(row, "id")),
      title: d1_require_field(row, "title").cast(),
      slug: d1_require_field(row, "slug").cast(),
      excerpt: (if @core.is_null(row["excerpt"]) || @core.typeof_(row["excerpt"]) == "undefined" { None } else { Some(row["excerpt"].cast()) }),
      status: d1_require_field(row, "status").cast(),
      published_at: (if @core.is_null(row["published_at"]) || @core.typeof_(row["published_at"]) == "undefined" { None } else { Some(row["published_at"].cast()) }),
      created_at: d1_require_field(row, "created_at").cast(),
      author_id: d1_read_int64(d1_require_field(row, "author_id")),
      author_username: d1_require_field(row, "author_username").cast(),
      author_display_name: d1_require_field(row, "author_display_name").cast(),
    })
  }
  results
}
